<view class="page">
    <view class="page__bd">
        <mp-cells>
            <mp-cell>
                <mp-uploader bindfail="uploadError" bindsuccess="uploadSuccess" select="{{selectFile}}" upload="{{uplaodFile}}" files="{{files}}" max-count="1"></mp-uploader>
            </mp-cell>
        </mp-cells>
    </view>
    <view class="btn-area button-sp-area">
      <button disabled="{{disabled}}" style="margin: 100rpx 0; background: #e24c27" type="primary" bindtap="submit">提交</button>
    </view>
</view>
